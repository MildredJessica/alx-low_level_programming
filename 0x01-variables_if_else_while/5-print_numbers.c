#include <stdio.h>

/**
 * main - Entry point
 * Prints all the numbers in base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num = 0;

	for (; num < 10; num++)
	{
		printf("%d",num);
	}
	printf("\n");
	return (0);
}
