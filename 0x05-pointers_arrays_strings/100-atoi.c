#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - Converts a string to an integer
 * @s: Parameter
 * Return: Integer
 */
int _atoi(char *s)
{
	unsigned int num;

	num = atoi(s);
	return (num);
}
