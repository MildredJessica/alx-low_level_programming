#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Returns the length of a character
 * @s: Parameter
 * Return: Length of a character
 */
int _strlen(char *s)
{
	return (strlen(s));
}
